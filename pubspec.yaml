name: salon_booking
description: "Prenotazioni premium per salone"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.2

dependencies:
  cloud_firestore: ^6.9.0
  cloud_functions: ^6.4.0
  cupertino_icons: ^1.0.9
  firebase_analytics: ^12.5.0
  firebase_app_check: ^0.4.7
  firebase_auth: ^6.6.1
  firebase_core: ^4.14.0
  firebase_crashlytics: ^5.3.0
  firebase_messaging: ^16.6.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^3.4.2
  go_router: ^18.0.0
  intl: ^0.20.3
  timezone: ^0.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
